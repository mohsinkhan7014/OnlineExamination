<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="nit.com.onlineexamination">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CandidateDetailesActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".IndexActivity" />
        <activity android:name=".UserRegistrationMainActivity" />
        <activity android:name=".CandidateMainActivity" />
        <activity android:name=".DummyAdminMainActivity" />
        <activity android:name=".TestMainActivity" />
        <activity android:name=".TestStartActivity" />
        <activity android:name=".AdminRegistrationMainActivity" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".CandidateDetails" />
        <activity android:name=".QuestionSetting" />
        <activity android:name=".ViewQuestionMain" />
        <activity android:name=".ViewQuestion"></activity>
    </application>

</manifest>